CREATE TABLE IF NOT EXISTS `PREFIX_printify_blueprint` (
  `printify_blueprint_id` INT(11) NOT NULL,
  `brand` VARCHAR(255) NOT NULL,
  PRIMARY KEY (`printify_blueprint_id`)
) ENGINE=ENGINE_TYPE DEFAULT CHARSET=utf8;
